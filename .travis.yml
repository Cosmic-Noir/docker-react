language: generic
sudo: require
services:
  - docker

before_install:
  - docker build -t kdoucette/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kdoucette/docker-react npm run test -- --coverage